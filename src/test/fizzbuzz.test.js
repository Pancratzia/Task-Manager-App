import { describe, expect, it } from 'vitest';

const fizzbuzz = (num) => {
    
}

describe('fizzbuzz', ()  => {

    it('should be a function', () => {
        expect(typeof fizzbuzz).toBe('function');
    })
})